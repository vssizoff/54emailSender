export type Entry = {
  firstName: string,
  lastName: string,
  name3: string,
  email: string,
  status: number
};

export type EmailConfigType = {
  mailHost: string,
  mailUser: string,
  mailPass: string,
  mailPort: number,
  mailSecure: boolean
};